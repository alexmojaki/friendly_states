dist: xenial
language: python

python:
  - 3.7

install:
  - pip install coveralls
  - pip install -e .[tests]
script:
  - coverage run pytest --include=friendly_states
  - coverage report -m

after_success:
  - coveralls

notifications:
  email: false
